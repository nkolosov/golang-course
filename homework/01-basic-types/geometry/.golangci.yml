# .golangci.yml
version: "2"
run:
  timeout: 5m
  tests: true

linters:
  default: all
  disable:
    - exhaustruct
    - wsl
    - depguard
    - forbidigo
    - wsl_v5
    - varnamelen
  settings:
    gocognit:
      min-complexity: 20
    gocyclo:
      min-complexity: 15

